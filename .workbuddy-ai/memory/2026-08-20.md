# 2026-08-20 工作日志

## Task 1: 安全加固 (Security Hardening) - 已完成
- 修改 `src-tauri/tauri.conf.json`：CSP 从 `null` 收紧为严格策略：`default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self' ipc: localhost:*;`
- 修改 `src-tauri/capabilities/default.json`：
  - 移除 `shell:allow-execute`（未使用）
  - 移除 `fs:default`（过于宽泛，保留 `fs:allow-read`）
  - 收窄 `opener:allow-open-path` 从 `/**` 改为 `$HOME/**`、`$APPDATA/**`、`$TEMP/**`
- 验证：`cargo check` 通过，无编译错误。

## Task 2: 工程化基建补齐 - 已完成
- 新增 `eslint.config.js`：ESLint 10 Flat Config + Vue + TypeScript + Prettier 集成。
- 新增 `.prettierrc`：2 空格缩进、无分号、单引号。
- 修改 `package.json`：
  - 补充 `eslint-plugin-vue`、`typescript-eslint` 到 devDependencies。
  - 新增脚本：`lint`、`lint:fix`、`format`、`type-check`、`test:rust`、`build:all`。
- 重写 `README.md`：替换 Tauri 官方模板为项目专属内容（功能概览、技术栈、快速开始、构建命令）。
- 验证：`vue-tsc --noEmit` 发现大量 pre-existing 类型错误（与本次修改无关，系 Vue SFC 类型声明缺失）。

## Task 3: 核心测试补充 (Rust 单元测试) - 已完成
- 重构 `parse_junit_results`：提取 `parse_junit_results_from_str` 纯字符串解析函数，原函数行为不变（先读文件再调用新函数）。
- 在 `commands.rs` 底部新增 `#[cfg(test)] mod tests` 模块，包含：
  - 7 个 JUnit XML 解析器测试：passed / failed / error / skipped / 多 case / 空 XML / ID 生成。
  - 6 个 Coverage JSON 解析器测试：format 1 / format 2 / 多文件排序 / 缺失字段 / 非法 JSON / 缺失 totals/files。
- 验证：`cargo check --tests` 编译通过（无类型/语法错误）。`cargo test` 因沙箱缺少 MSVC 链接器无法执行，但代码已就绪。
